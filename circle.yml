machine:
  node:
    version: 7.7.2

dependencies:
  override:
    - npm install elm create-elm-app
    - npm install

  cache_directories:
      - "node_modules"

compile:
  override:
    - elm-app build
