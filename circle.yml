machine:
  node:
    version: 7.7.2

dependencies:
  override:
    - npm install elm create-elm-app -g
    - npm install

compile:
  override:
    - elm-app build

deployment:
  production:
    branch: master
    commands:
    - ./deploy-ghpages.sh build
